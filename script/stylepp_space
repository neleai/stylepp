DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

for I in  trailing_space form_feed space_before_tab leading_space; do
	$DIR/stylepp_skeleton stylepp_$I
	$DIR/../gnu/mkchlog Formatted by $I.
	git commit -a -m "Formatted by $I."
done
