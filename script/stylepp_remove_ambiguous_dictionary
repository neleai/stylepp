while read WORD; do
  if [ "`echo $WORD | awk '{print $1;}'`" !=  "`echo $LASTWORD | awk '{print $1;}'`" ]; then
    if [ "$WRD" != "dupe" ]; then
      echo $WRD
    fi
    WRD=$WORD
  else
    WRD="dupe"
  fi
  LASTWORD=$WORD
done
   if [ "$WRD" != "dupe" ]; then
      echo $WRD
    fi

