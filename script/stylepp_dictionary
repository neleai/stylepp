DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PATH=$PATH:$DIR/../bin:$DIR

cp /tmp/misspells .
cp misspells corrected
aspell -c corrected
stylepp_make_dictionary misspells corrected > dictionary
