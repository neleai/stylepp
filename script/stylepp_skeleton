DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

for CUP in $*; do

  for I in `find -regex ".*[.][ch]\$"`; do

    if $DIR/bin/$CUP $I <$I > /tmp/fmt; then
      echo $I
      mv /tmp/fmt $I
    fi
  done
done

