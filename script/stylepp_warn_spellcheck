DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PATH=$PATH:$DIR/../bin:$DIR

CHECK=`stylepp_extract_comment $1 < $1 | aspell list`
if [ -z "$CHECK" ]; then
  true
else
  echo $1: Misspellings found by aspell: $CHECK. >&2
fi
exit 1
